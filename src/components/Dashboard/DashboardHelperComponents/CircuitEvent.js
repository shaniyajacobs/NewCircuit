const CircuitEvent = (remoEvent, menCapacity, womenCapacity) => {
  return {
    remoEvent: remoEvent,
    menCapacity: menCapacity,
    womenCapacity: womenCapacity,
  }
}

export default CircuitEvent;